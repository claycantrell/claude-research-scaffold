# Semantic Scholar API (free — request at https://www.semanticscholar.org/product/api#Partner-Form)
SEMANTIC_SCHOLAR_API_KEY=

# LLM API keys — only needed for empirical projects that call models
# (labeling, summarization, generation experiments). Leave blank otherwise.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
